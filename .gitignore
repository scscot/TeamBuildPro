# === General / OS Specific Ignores ===
.DS_Store
.AppleDouble
.LSOverride
# Vim swap files
*.swp
# Python bytecode
*.pyc
# Atom editor specific files
.atom/
# History files (e.g., shell history within project)
.history
# Subversion (SVN) version control directories
.svn/
# macOS file system events daemon temporary files
.fseventsd
# Spotlight indexing temporary files
.Spotlight-V100
# macOS trash folder
.Trashes
# Common temporary build directories
.build/
.buildlog/
migrate_working_dir/ # Specific to user's project
*.class # Java compiled files (if applicable)
*.log # Generic log files
# Specific log files from Flutter (e.g., flutter_01.log)
flutter_*.log

# === Environment Variables and Secrets ===
# DO NOT commit sensitive environment variables or secrets.
# Exclude .env files in various formats and locations.
.env
.env.*
assets/env.*
# Exception: Keep the template file for environment variables if it exists.
!assets/env.template
secrets/ # Directory for secrets, should almost always be ignored.
# Firebase configuration file (environment/user specific)
.firebaserc

# === IDE Specific Ignores ===
# IntelliJ / Android Studio
.idea/
*.iml
*.ipr
*.iws

# VS Code
# Uncomment the line below if you want to ignore VS Code's user-specific
# settings, launch configurations, and tasks. It's often good for team projects.
#.vscode/

functions/
node_modules/
secrets/

# === Flutter / Dart / Pub Specific Ignores ===
# Flutter build output directory
build/
# Dart toolchain cache and generated files (covers .dart_tool, .dart_tool 2, etc.)
.dart_tool*
# Pub cache and package resolution files
.pub/
.packages
# Flutter plugin-related files
.flutter-plugins
.flutter-plugins-dependencies
.flutter-plugins-generated
# Generated API documentation for Dart packages
**/doc/api/
# Specific Flutter-generated image files (if not intended for tracking)
flutter_*.png # e.g., flutter_logo.png if auto-generated
# Flutter generated metadata
.metadata

# === iOS / Xcode Specific Ignores ===
# Xcode derived data (large build artifacts)
DerivedData/
# Xcode user state files (user-specific project settings)
*.xcuserstate
*.pbxuser
*.xcscmblueprint
*.xccheckout
*.mode1v3
*.mode2v3
*.perspectivev3
# Xcode archives (for distribution)
*.xcarchive
*.ipa
*.dSYM.zip
*.app.dSYM
*.framework.dSYM
# Backup files generated by `sed -i` on macOS (if not `sed -i ''`)
*.bak
*.orig
*.xcconfig.backup # Backups of xcconfig files
mybuildsettings_snapshot.txt # Specific to user's project
tbp_files.zip # Specific to user's project
DartFiles.zip # Specific to user's project
# Certificate files (should always be ignored)
*.cer
# Ignore backup directories created for iOS
ios_backup*/

# Xcode Workspace specific ignores
# NOTE: The .xcworkspace itself (e.g., Runner.xcworkspace/) should generally be tracked
# for projects using CocoaPods or custom configurations.
# We ignore only user-specific or temporary files within it.
*.xcworkspace/contents.xcworkspacedata # This *should* usually be tracked for shared workspace settings.
*.xcworkspace/xcshareddata/WorkspaceSettings.xcsettings # These are shared settings, should usually be tracked.
# The following lines ignore user-specific data within the workspace:
*.xcworkspace/xcuserdata/

# Pods and related files (managed by CocoaPods)
ios/Pods/
ios/.symlinks/
ios/.generated/ # Flutter/Firebase generated files in ios/

# Generated Xcode config files (specific to Flutter's iOS runner)
ios/Flutter/Generated.xcconfig
ios/Flutter/flutter_export_environment.sh
# Broader pattern for generated xcconfigs and shell scripts
ios/Flutter/Generated*.xcconfig
ios/Flutter/*.sh # Catches flutter_export_environment*.sh and others
# Backup of Runner.xcworkspace if duplicated
ios/Runner 2.xcworkspace/ # Ignore any duplicate workspace directories
ios/Runner 2.xcworkspace/xcshareddata/
ios/Runner 2.xcworkspace/contents.xcworkspacedata

# === Platform-Specific Generated Files ===
# Generated plugin registrants and cmake files within platform folders
# NOTE: The root `windows/`, `linux/`, `macos/` directories themselves
# should generally be tracked as they contain project source code.
# Only generated build artifacts or project files should be ignored.
windows/flutter/generated_plugin_registrant.cc
windows/flutter/generated_plugins.cmake
linux/flutter/generated_plugin_registrant.cc
linux/flutter/generated_plugins.cmake
macos/Flutter/GeneratedPluginRegistrant.swift

# === Files That Should Typically BE COMMITTED (and therefore NOT ignored) ===
# These are often mistakenly ignored but are vital for team collaboration
# and consistent builds across different development environments.
# They are commented out below to show they should NOT be in .gitignore.

# project.pbxproj: The main Xcode project file.
# ios/Runner.xcodeproj/project.pbxproj

# Podfile.lock: Locks CocoaPods dependency versions for consistency.
# ios/Podfile.lock

# Entitlements: Defines app capabilities (Push Notifications, Background Modes, etc.).
# ios/Runner/Runner.entitlements

# Info.plist: App metadata, permissions, and launch configurations.
# ios/Runner/Info.plist

# Git-related files that should always be committed
# .gitignore
# .gitattributes